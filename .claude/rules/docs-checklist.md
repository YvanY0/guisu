---
paths:
  - "docs/src/**"
  - "README.md"
---

# Documentation Checklist

User-facing feature work must update `docs/src/developer-guide/contributing.md` "Documentation" checklist before merge — it already enumerates the pages to update (commands, template functions, config keys, file attributes, hooks, CLI flags, shell completion). PRs are not exempt.
